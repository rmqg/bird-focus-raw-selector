# v1.2 验证记录（抽样 200 张）

验证日期：2026-03-28  
源目录：`E:\100NZ7_2`  
口径：`copy` 实际复制、`fast` 预设、`--sample-limit 200`

## 统一参数说明

- 设备：源码测试分别使用 `cpu` 与 `0(gpu)`；便携包分别使用 CPU/GPU 启动脚本
- 递归扫描：开启
- 目录排除：`selected_birds_in_focus,raw`
- 模型：`yolov8s-seg.pt`

## 四项结果

| 测试项 | 处理数 | 入选/复制 | 用时 (s) | 平均速度 (张/s) | 备注 |
|---|---:|---:|---:|---:|---|
| 源码 CPU | 200 | 25 | 106.07 | 1.8856 | `--cpu-workers 0`（自动使用全部逻辑核心） |
| 源码 GPU | 200 | 25 | 75.91 | 2.6347 | `--device 0` |
| 便携包 CPU | 200 | 25 | 109.30 | 1.8298 | 内置 Python，真实复制 |
| 便携包 GPU（冷启动） | 200 | 25 | 583.00 | 0.3431 | 含首次创建 venv + 安装 GPU 依赖耗时 |

补充：便携 GPU 在依赖安装完成后，单看扫描处理阶段约 `69s/200`，约 `2.9 张/s`。

## 结论

- 四项测试在同一参数口径下，入选结果一致（均为 25/200）。
- CPU 全核自动并行已生效（`--cpu-workers 0`）。
- 便携 GPU 的首次运行耗时主要来自依赖安装，不是识别算法本身。
