鸟类 RAW 初筛工具（CPU 便携包）使用说明

这是 CPU 专用便携包。
本包已内置 Python 运行环境。

使用步骤：
1) 解压整个 zip 到本地磁盘（不要在压缩包内直接运行）。
2) 按需执行：Run_DryRun_Fast_CPU.bat（预览结果，不复制）。
3) 按需执行：Run_Copy_Fast_CPU.bat（正式复制）。

提示：
- 启动后先选源文件夹（RAW 根目录）。
- 再选输出文件夹（可直接回车用默认）。
- 最后设置日志文件路径（可直接回车自动生成）。
