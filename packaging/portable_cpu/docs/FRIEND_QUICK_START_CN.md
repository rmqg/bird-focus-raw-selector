# CPU 便携包快速使用说明

## 1. 先预览再复制

1. 双击 `Run_DryRun_Fast_CPU.bat`，先看筛选结果（不复制）。
2. 确认结果后，双击 `Run_Copy_Fast_CPU.bat` 执行正式复制。

## 2. 运行时输入

- 源目录：RAW 根目录（会递归扫描）。
- 输出目录：筛选命中的 RAW 复制到这里。
- 日志路径：保存每张图的结构化结果（CSV）。

## 3. 说明

- 本包仅使用 CPU。
- 会自动跳过 `selected_birds_in_focus*` 与 `raw*` 子目录。
